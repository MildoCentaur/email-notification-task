spring:
  profiles:
    active: default, dev
  cloud:
    config:
      uri: http://localhost:8088
      username: ${username}
      password: ${password}
  application:
    name: email-notification-task
encrypt:
  key: ${key}